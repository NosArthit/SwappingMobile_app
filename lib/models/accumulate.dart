class Accumulate{
  String? accumulateCharging;
  String? accumulateDischarging;
  String? masterTimer;

  Accumulate({
    required this.accumulateCharging,
    required this.accumulateDischarging,
    required this.masterTimer
  });
}